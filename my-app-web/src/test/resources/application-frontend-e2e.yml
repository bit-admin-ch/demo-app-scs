server:
  port: 18080

spring:
  datasource:
    url: jdbc:h2:mem:frontend-browser-it;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  flyway:
    enabled: false